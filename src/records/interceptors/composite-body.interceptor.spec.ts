import { CompositeBodyInterceptor } from './composite-body.interceptor';

describe('CompositeBodyInterceptor', () => {
  it('should be defined', () => {
    expect(new CompositeBodyInterceptor()).toBeDefined();
  });
});
