import { MakeRelationBodyGuard } from './make-relation-body.guard';

describe('MakeRelationBodyGuard', () => {
  it('should be defined', () => {
    expect(new MakeRelationBodyGuard()).toBeDefined();
  });
});
